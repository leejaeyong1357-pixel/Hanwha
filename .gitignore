node_modules/
.next/
out/
.env
.env.local
*.log
.DS_Store
# 문항 음성은 저장소에 넣는다 (204MB).
# Cloudflare Pages 가 GitHub 에서 바로 빌드하려면 여기 있어야 하고,
# 생성에 CPU 로 한 시간이 걸려 매번 다시 만들 수 없다.
# 파일 이름이 문장 해시라 뱅크를 다시 만들어도 대부분 그대로 재사용된다.
tsconfig.tsbuildinfo
next-env.d.ts
prisma/dev.db
services/tts/models/
__pycache__/
*.pyc

# 빌드 때 data/ 에서 복사한다
public/data/
