# 이 저장소는 한화엔진 배포다. lib/brand.ts 가 이 값으로 문구·색·로고·아이콘을 고른다.
#
# 단국대 배포(Dankuk 저장소)에는 이 파일이 없다 — 값이 없으면 단국대가 기본이다.
# 두 저장소의 차이는 이 파일 하나뿐이므로, 코드를 옮겨올 때 이 파일만 지키면 된다.
#
# 비밀값은 여기에 두지 말 것 — 저장소에 그대로 올라간다.
NEXT_PUBLIC_BRAND=hanwha
